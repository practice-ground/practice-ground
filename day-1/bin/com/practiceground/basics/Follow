On Jshell 
==========
1) first explain data types 
2) conditions
3) loops 

4) methods 
5) paramter methods 
6) class 
7) and migrate the method in the class 

8) show compile and running the program on console. 
 
9 ) JVM ( java virtual machine ) runs java bytecode
JRE = JVM + libraries + other components 
JDK = JRE + compilers + Debuggers 


